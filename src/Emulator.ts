class Emulator {
  id: number;
  name: string;
  dirMedia: string;
  constructor(id: number, name: string, dirMedia: string) {
    this.id = id;
    this.name = name;
    this.dirMedia = dirMedia;
  }
}

export default Emulator;
